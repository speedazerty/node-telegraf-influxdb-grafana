{
  "name": "test-grafana",
  "version": "1.0.0",
  "description": "",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "rm -rf ./build && tsc && node build/index.js",
    "start:dev": "NODE_ENV=development ./node_modules/.bin/nodemon --watch '*' --exec 'ts-node' index.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/statsd-client": "^0.1.30",
    "statsd-client": "^0.2.2",
    "ts-node": "^1.6.1",
    "tslint": "^3.15.1",
    "typescript": "^2.0.3",
    "typings": "^1.4.0"
  },
  "devDependencies": {
    "nodemon": "^1.11.0"
  }
}
